{
  "name": "mottainai-flow-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "oxlint . && biome check .",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "@biomejs/biome": "^1.9.0",
    "oxlint": "^0.12.0"
  }
}
